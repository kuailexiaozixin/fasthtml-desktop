python-fasthtml>=0.12.0
fastapi>=0.115
uvicorn>=0.30.0
sqlalchemy>=2.0
python-dotenv>=1.0
markdown>=3.6
httpx>=0.27
python-multipart>=0.0.9
pywebview>=5.0
# 说明：上游依赖 psycopg2-binary（PostgreSQL 驱动）。桌面示例已把 db.py 改为
# SQLite 后端（零外部服务、双击即跑），故移除该驱动。若要还原上游 PostgreSQL 行为，
# 自行 pip install psycopg2-binary 并设置 DB_URL=postgresql://... + FASTLMS_SCHEMA=fastlms
